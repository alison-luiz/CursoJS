// Object.values()
// Object.entries()

const produto = { 
    nome: 'Produto', 
    preco: 5 
};

console.log(Object.values(produto)); // Retorna somente os valores dos objetos
console.log(Object.entries(produto)); // Retorna as chaves e os valores do objeto
